# Input documents

Place private PDF or image inputs below this directory. Nested directories are supported.
Everything except this guide is ignored by Git. Never add real invoices to source control.


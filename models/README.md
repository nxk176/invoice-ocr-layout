# Model files

Download declared public base checkpoints with `python scripts/download_models.py`.
Weights and checkpoints below this directory are ignored by Git.

Fine-tuned invoice checkpoints do not exist until training has produced them. The framework
will report a missing checkpoint instead of silently falling back or fabricating output.

